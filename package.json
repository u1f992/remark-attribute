{
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "format": "npm run format --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/micromark-extension-attribute",
    "packages/mdast-util-attribute",
    "packages/remark-attribute"
  ],
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^22.19.10",
    "eslint": "^9.39.2",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0"
  }
}
